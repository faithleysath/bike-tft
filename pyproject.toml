[project]
name = "bike-tft-next"
version = "0.1.0"
description = "Clean-room workspace for bike sharing forecasting, rebalancing, visualization, and thesis assets."
requires-python = ">=3.13"
dependencies = [
    "kaggle>=2.1.0",
]

[tool.uv]
package = false
